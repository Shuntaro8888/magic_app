<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <%= render 'layouts/rails_default' %> <!-- Rails default partial -->
  </head>
  <body>
    <%= render 'layouts/header' %> <!-- Header partial -->
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %> <!-- Footer partial -->
    </div>
  </body>
</html>
